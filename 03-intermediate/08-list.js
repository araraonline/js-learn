// I will set up an interface for a list

class List {
    constructor(memory) {
        // construct things and set length
    }

    // modifying the structure of the array

    append(value) {
    }

    pop() {
    }

    // modifying values in the array

    get(index) {
    }

    set(index, value) {
    }
}
